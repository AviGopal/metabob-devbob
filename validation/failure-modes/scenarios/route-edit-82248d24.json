{
  "id": "route-edit-82248d24",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "In repos/identity-vessel/src/resolvers/issue-key.ts, split the key-minting body out of issueApiKey() into a new exported",
  "description": "In repos/identity-vessel/src/resolvers/issue-key.ts, split the key-minting body out of issueApiKey() into a new exported function mintApiKey() that performs NO authorization check.\n\nThe file currently contains exactly these five lines in sequence (quoted verbatim):\n\n/** Issue a new API key. Admin-only. */\nexport async function issueApiKey(\n  body: unknown,\n  authHeader: string | undefined,\n): Promise<IssueKeyResult> {\n  const authz = await authorizeAdmin(authHeader);\n  if ('status' in authz) return authz;\n\n  const validated = validateInput(body);\n\nReplace ONLY the final line of that block — the line reading exactly:\n  const validated = validateInput(body);\n— and the blank line immediately before it, with this text:\n\n  return mintApiKey(body);\n}\n\n/**\n * Mint + persist a key, WITHOUT any authorization check.\n *\n * Split out of issueApiKey() so a caller that has established authority by some\n * other means can reuse the exact minting path rather than reimplementing it.\n * The only such caller is the loopback admin-bootstrap resolver, which proves\n * in-container root by presenting API_KEY_SECRET.\n *\n * Anything reachable from the network MUST go through issueApiKey() instead.\n * This function grants an arbitrary scope set, including 'admin', to any caller.\n */\nexport async function mintApiKey(body: unknown): Promise<IssueKeyResult> {\n  const validated = validateInput(body);\n\nDo not change anything else. The remainder of the original function body — everything from the line `  if ('status' in validated) return validated;` through the closing `}` of the function — stays exactly as it is and becomes the body of mintApiKey. The existing exported symbol issueApiKey must keep its current signature and behaviour (authorizeAdmin first, then mint).\n\nThis must typecheck. Verify with: node node_modules/typescript/bin/tsc --noEmit",
  "goal_text": "In repos/identity-vessel/src/resolvers/issue-key.ts, split the key-minting body out of issueApiKey() into a new exported function mintApiKey() that performs NO authorization check.\n\nThe file currently contains exactly these five lines in sequence (quoted verbatim):\n\n/** Issue a new API key. Admin-only. */\nexport async function issueApiKey(\n  body: unknown,\n  authHeader: string | undefined,\n): Promise<IssueKeyResult> {\n  const authz = await authorizeAdmin(authHeader);\n  if ('status' in authz) return authz;\n\n  const validated = validateInput(body);\n\nReplace ONLY the final line of that block — the line reading exactly:\n  const validated = validateInput(body);\n— and the blank line immediately before it, with this text:\n\n  return mintApiKey(body);\n}\n\n/**\n * Mint + persist a key, WITHOUT any authorization check.\n *\n * Split out of issueApiKey() so a caller that has established authority by some\n * other means can reuse the exact minting path rather than reimplementing it.\n * The only such caller is the loopback admin-bootstrap resolver, which proves\n * in-container root by presenting API_KEY_SECRET.\n *\n * Anything reachable from the network MUST go through issueApiKey() instead.\n * This function grants an arbitrary scope set, including 'admin', to any caller.\n */\nexport async function mintApiKey(body: unknown): Promise<IssueKeyResult> {\n  const validated = validateInput(body);\n\nDo not change anything else. The remainder of the original function body — everything from the line `  if ('status' in validated) return validated;` through the closing `}` of the function — stays exactly as it is and becomes the body of mintApiKey. The existing exported symbol issueApiKey must keep its current signature and behaviour (authorizeAdmin first, then mint).\n\nThis must typecheck. Verify with: node node_modules/typescript/bin/tsc --noEmit",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-82248d24",
  "source_gap_source": "substrate_detected"
}