{
  "id": "solicitation-outcome-scan-reads-a-surface-the-answers-never-reach",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "Edit repos/development-vessel/src/resolvers/solicitation-outcome-scan.ts. This is ONE contiguous string replacement insi",
  "description": "Edit repos/development-vessel/src/resolvers/solicitation-outcome-scan.ts. This is ONE contiguous string replacement inside that single file. Do not add or change any import statement — the new code deliberately uses Bun.file so no new imports are needed.\n\nReplace this exact existing block:\n\nexport async function resolveSolicitationOutcomeScan(pointer: SolicitationOutcomeScanPointer): Promise<ResolverResult> {\n  const ids = Array.isArray(pointer.solicitation_ids) ? pointer.solicitation_ids : [];\n  const horizon = typeof pointer.horizon_ms === \"number\" ? pointer.horizon_ms : 14400000;\n  if (ids.length === 0) {\n    return { shape: \"solicitationOutcomeReport\", body: { authored: true, outcomes: [], answered: 0, episode_count: 0, note: \"no solicitation_ids supplied\" } };\n  }\n\nwith this exact replacement:\n\nexport async function resolveSolicitationOutcomeScan(pointer: SolicitationOutcomeScanPointer): Promise<ResolverResult> {\n  let ids = Array.isArray(pointer.solicitation_ids) ? pointer.solicitation_ids : [];\n  const horizon = typeof pointer.horizon_ms === \"number\" ? pointer.horizon_ms : 14400000;\n  // SELF-SUPPLY (2026-08-28): the only caller is boredom-vessel's \"human-interacting\" family\n  // list, which dispatches this shape BARE. ids was therefore always empty and the scan returned\n  // \"no solicitation_ids supplied\" every time -- a guaranteed no-op, verified live. When no ids\n  // are supplied, read the outstanding escalation panels from stateful-ui's uiQuestion read shape.\n  if (ids.length === 0) {\n    try {\n      const uiEndpoint = process.env[\"STATEFUL_UI_VESSEL_ENDPOINT\"] ?? \"http://127.0.0.1:8270\";\n      const uiRes = await fetch(uiEndpoint + \"/resolve\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ impulse: { pointer: { type: \"uiQuestion\" } } }), signal: AbortSignal.timeout(10000) });\n      const uiJson = (await uiRes.json()) as { body?: { questions?: Array<{ id?: string }> } };\n      ids = (uiJson.body?.questions ?? []).map((q) => String(q.id ?? \"\")).filter((qid) => qid.startsWith(\"needs-human-\") || qid.startsWith(\"reland-needs-human-\"));\n    } catch { /* transport failure -> fail open, ids stays empty */ }\n  }\n  if (ids.length === 0) {\n    return { shape: \"solicitationOutcomeReport\", body: { authored: true, outcomes: [], answered: 0, episode_count: 0, note: \"no outstanding solicitations\" } };\n  }\n  // ANSWER SURFACE (2026-08-28): a human answering an escalation panel hits stateful-ui-vessel's\n  // POST /api/feedback, which emits uiFeedback_write keyed by panel_id; dev-vessel's interactor\n  // passthrough appends that to WORKSPACE_ROOT/interactor-log/uiFeedback_write.jsonl. That file is\n  // the durable answer record -- this is the \"gap-consumer learns to read the log files\" step the\n  // interactor-passthrough module comment describes. The obsidian:interaction_episode read below\n  // matches a DIFFERENT surface keyed on solicitation_ids and is retained for episode-based\n  // solicitations that genuinely carry them; panel escalations never appear there.\n  const answeredPanels = new Set<string>();\n  try {\n    const root = process.env[\"WORKSPACE_ROOT\"] ?? \"/workspace\";\n    const logFile = Bun.file(root + \"/interactor-log/uiFeedback_write.jsonl\");\n    if (await logFile.exists()) {\n      for (const line of (await logFile.text()).split(\"\\n\")) {\n        if (!line.trim()) continue;\n        try {\n          const rec = JSON.parse(line) as { pointer?: { panel_id?: string; kind?: string } };\n          const pid = rec.pointer?.panel_id;\n          if (typeof pid === \"string\" && pid && rec.pointer?.kind !== \"dismiss\") answeredPanels.add(pid);\n        } catch { /* skip malformed line */ }\n      }\n    }\n  } catch { /* fail open -> answeredPanels stays empty */ }\n\nThen, separately in the same file, in the existing `const outcomes = ids.map((id) => {` callback, insert this as the FIRST statement inside the callback body, immediately before the existing line `if (episodes === null) return { solicitation_id: id, outcome: \"unscored_absent\" as const };`:\n\n    if (answeredPanels.has(id)) { answered += 1; return { solicitation_id: id, outcome: \"answered\" as const }; }\n\nDo not change anything else. Do not touch the `recordOperatorEngagement` call or its `reland-needs-human-` prefix condition — that tally calibrates a different evidence class and must stay as it is.",
  "goal_text": "Edit repos/development-vessel/src/resolvers/solicitation-outcome-scan.ts. This is ONE contiguous string replacement inside that single file. Do not add or change any import statement — the new code deliberately uses Bun.file so no new imports are needed.\n\nReplace this exact existing block:\n\nexport async function resolveSolicitationOutcomeScan(pointer: SolicitationOutcomeScanPointer): Promise<ResolverResult> {\n  const ids = Array.isArray(pointer.solicitation_ids) ? pointer.solicitation_ids : [];\n  const horizon = typeof pointer.horizon_ms === \"number\" ? pointer.horizon_ms : 14400000;\n  if (ids.length === 0) {\n    return { shape: \"solicitationOutcomeReport\", body: { authored: true, outcomes: [], answered: 0, episode_count: 0, note: \"no solicitation_ids supplied\" } };\n  }\n\nwith this exact replacement:\n\nexport async function resolveSolicitationOutcomeScan(pointer: SolicitationOutcomeScanPointer): Promise<ResolverResult> {\n  let ids = Array.isArray(pointer.solicitation_ids) ? pointer.solicitation_ids : [];\n  const horizon = typeof pointer.horizon_ms === \"number\" ? pointer.horizon_ms : 14400000;\n  // SELF-SUPPLY (2026-08-28): the only caller is boredom-vessel's \"human-interacting\" family\n  // list, which dispatches this shape BARE. ids was therefore always empty and the scan returned\n  // \"no solicitation_ids supplied\" every time -- a guaranteed no-op, verified live. When no ids\n  // are supplied, read the outstanding escalation panels from stateful-ui's uiQuestion read shape.\n  if (ids.length === 0) {\n    try {\n      const uiEndpoint = process.env[\"STATEFUL_UI_VESSEL_ENDPOINT\"] ?? \"http://127.0.0.1:8270\";\n      const uiRes = await fetch(uiEndpoint + \"/resolve\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ impulse: { pointer: { type: \"uiQuestion\" } } }), signal: AbortSignal.timeout(10000) });\n      const uiJson = (await uiRes.json()) as { body?: { questions?: Array<{ id?: string }> } };\n      ids = (uiJson.body?.questions ?? []).map((q) => String(q.id ?? \"\")).filter((qid) => qid.startsWith(\"needs-human-\") || qid.startsWith(\"reland-needs-human-\"));\n    } catch { /* transport failure -> fail open, ids stays empty */ }\n  }\n  if (ids.length === 0) {\n    return { shape: \"solicitationOutcomeReport\", body: { authored: true, outcomes: [], answered: 0, episode_count: 0, note: \"no outstanding solicitations\" } };\n  }\n  // ANSWER SURFACE (2026-08-28): a human answering an escalation panel hits stateful-ui-vessel's\n  // POST /api/feedback, which emits uiFeedback_write keyed by panel_id; dev-vessel's interactor\n  // passthrough appends that to WORKSPACE_ROOT/interactor-log/uiFeedback_write.jsonl. That file is\n  // the durable answer record -- this is the \"gap-consumer learns to read the log files\" step the\n  // interactor-passthrough module comment describes. The obsidian:interaction_episode read below\n  // matches a DIFFERENT surface keyed on solicitation_ids and is retained for episode-based\n  // solicitations that genuinely carry them; panel escalations never appear there.\n  const answeredPanels = new Set<string>();\n  try {\n    const root = process.env[\"WORKSPACE_ROOT\"] ?? \"/workspace\";\n    const logFile = Bun.file(root + \"/interactor-log/uiFeedback_write.jsonl\");\n    if (await logFile.exists()) {\n      for (const line of (await logFile.text()).split(\"\\n\")) {\n        if (!line.trim()) continue;\n        try {\n          const rec = JSON.parse(line) as { pointer?: { panel_id?: string; kind?: string } };\n          const pid = rec.pointer?.panel_id;\n          if (typeof pid === \"string\" && pid && rec.pointer?.kind !== \"dismiss\") answeredPanels.add(pid);\n        } catch { /* skip malformed line */ }\n      }\n    }\n  } catch { /* fail open -> answeredPanels stays empty */ }\n\nThen, separately in the same file, in the existing `const outcomes = ids.map((id) => {` callback, insert this as the FIRST statement inside the callback body, immediately before the existing line `if (episodes === null) return { solicitation_id: id, outcome: \"unscored_absent\" as const };`:\n\n    if (answeredPanels.has(id)) { answered += 1; return { solicitation_id: id, outcome: \"answered\" as const }; }\n\nDo not change anything else. Do not touch the `recordOperatorEngagement` call or its `reland-needs-human-` prefix condition — that tally calibrates a different evidence class and must stay as it is.",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "solicitation-outcome-scan-reads-a-surface-the-answers-never-reach",
  "source_gap_source": "substrate_detected"
}