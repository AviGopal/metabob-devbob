{
  "id": "route-edit-40fb14ac",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "Create the new file repos/goal-host-vessel/src/goal-intent.creation-routing.test.ts with EXACTLY this complete content a",
  "description": "Create the new file repos/goal-host-vessel/src/goal-intent.creation-routing.test.ts with EXACTLY this complete content and nothing else (do not abbreviate, stub, or add a placeholder — emit the full file verbatim):\n\nimport { isEditIntentGoal } from './goal-intent';\nimport { describe, expect, it } from 'bun:test';\n\ndescribe('isEditIntentGoal — creation verbs route as edit-intent (regression for 13f7440)', () => {\n  it('matches a creation verb with a concrete repos path', () => {\n    expect(isEditIntentGoal('Create repos/foo/bar.ts with a helper')).toBe(true);\n    expect(isEditIntentGoal('Author repos/x/y.ts')).toBe(true);\n  });\n  it('does not match a read goal that merely names a file', () => {\n    expect(isEditIntentGoal('Summarise repos/x/y.ts')).toBe(false);\n  });\n});",
  "goal_text": "Create the new file repos/goal-host-vessel/src/goal-intent.creation-routing.test.ts with EXACTLY this complete content and nothing else (do not abbreviate, stub, or add a placeholder — emit the full file verbatim):\n\nimport { isEditIntentGoal } from './goal-intent';\nimport { describe, expect, it } from 'bun:test';\n\ndescribe('isEditIntentGoal — creation verbs route as edit-intent (regression for 13f7440)', () => {\n  it('matches a creation verb with a concrete repos path', () => {\n    expect(isEditIntentGoal('Create repos/foo/bar.ts with a helper')).toBe(true);\n    expect(isEditIntentGoal('Author repos/x/y.ts')).toBe(true);\n  });\n  it('does not match a read goal that merely names a file', () => {\n    expect(isEditIntentGoal('Summarise repos/x/y.ts')).toBe(false);\n  });\n});",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-40fb14ac",
  "source_gap_source": "substrate_detected"
}