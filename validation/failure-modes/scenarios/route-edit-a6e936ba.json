{
  "id": "route-edit-a6e936ba",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "Edit repos/development-vessel/src/resolvers/docs-align-tick.ts. This is ONE string replacement, expressed below as an ex",
  "description": "Edit repos/development-vessel/src/resolvers/docs-align-tick.ts. This is ONE string replacement, expressed below as an exact old_string and new_string pair. Apply it literally.\n\nold_string (this exact single line, which occurs exactly once in the file):\nfunction walkScripts(dir: string, cap: number): string[] {\n\nnew_string (the helper, one blank line, then that same declaration line ONCE):\nasync function fetchAdvertisedShapes(): Promise<string[]> {\n  const base = (process.env[\"DISCOVERY_ENDPOINT\"] ?? \"http://127.0.0.1:8100\").replace(/\\/+$/, \"\");\n  try {\n    const res = await fetch(`${base}/registry/shapes`, { signal: AbortSignal.timeout(10_000) });\n    if (!res.ok) return [];\n    const j = (await res.json()) as unknown;\n    const arr = Array.isArray(j)\n      ? j\n      : Array.isArray((j as { shapes?: unknown })?.shapes)\n        ? (j as { shapes: unknown[] }).shapes\n        : [];\n    return arr.filter((s): s is string => typeof s === \"string\");\n  } catch {\n    return [];\n  }\n}\n\nfunction walkScripts(dir: string, cap: number): string[] {\n\nCRITICAL — THE EXACT MISTAKE A PREVIOUS ATTEMPT MADE: it built an old_string that contained the walkScripts declaration line TWICE in a row, producing a duplicated function declaration. The edit applied and was then correctly rejected with `unrelated_diff_orphan_declaration`. Do not repeat that. The old_string is ONE line only. The new_string ends with that same line appearing exactly ONCE, as the final line. After your edit the file must still contain exactly one occurrence of `function walkScripts(dir: string, cap: number): string[] {`, and the body of walkScripts must be untouched.\n\nDo not add or modify any import — process.env, fetch and AbortSignal are globals here. Do not touch walkMd, deriveNamingVocabulary, resolveDocsAlignTick, the live_truth object literal, or the invariants array.\n\nPURPOSE: this supplies the missing input for the accuracy invariant in docs-align-scan.ts, which is gated at line 236 on pointer.live_truth.advertised_shapes. docs-align-tick.ts builds live_truth as { existing_paths, unit_names } only — both from the local filesystem, exactly as its own comment says: \"Assemble live_truth from local filesystem only\". Because the tick never queries the discovery registry, advertised_shapes is permanently undefined and the accuracy check — the only invariant that compares a document against live truth — has never executed. A separate later change will pass this value into live_truth and add \"accuracy\" to the invariants array; do NOT do that now.\n\nEvery failure path returns an empty array deliberately, so if discovery is unreachable the accuracy branch stays exactly as inert as it is today rather than throwing. The `.replace(/\\/+$/, \"\")` endpoint normalisation matches the idiom already used in orphaned-capability-scan.ts and residual-shape-discovery.ts.",
  "goal_text": "Edit repos/development-vessel/src/resolvers/docs-align-tick.ts. This is ONE string replacement, expressed below as an exact old_string and new_string pair. Apply it literally.\n\nold_string (this exact single line, which occurs exactly once in the file):\nfunction walkScripts(dir: string, cap: number): string[] {\n\nnew_string (the helper, one blank line, then that same declaration line ONCE):\nasync function fetchAdvertisedShapes(): Promise<string[]> {\n  const base = (process.env[\"DISCOVERY_ENDPOINT\"] ?? \"http://127.0.0.1:8100\").replace(/\\/+$/, \"\");\n  try {\n    const res = await fetch(`${base}/registry/shapes`, { signal: AbortSignal.timeout(10_000) });\n    if (!res.ok) return [];\n    const j = (await res.json()) as unknown;\n    const arr = Array.isArray(j)\n      ? j\n      : Array.isArray((j as { shapes?: unknown })?.shapes)\n        ? (j as { shapes: unknown[] }).shapes\n        : [];\n    return arr.filter((s): s is string => typeof s === \"string\");\n  } catch {\n    return [];\n  }\n}\n\nfunction walkScripts(dir: string, cap: number): string[] {\n\nCRITICAL — THE EXACT MISTAKE A PREVIOUS ATTEMPT MADE: it built an old_string that contained the walkScripts declaration line TWICE in a row, producing a duplicated function declaration. The edit applied and was then correctly rejected with `unrelated_diff_orphan_declaration`. Do not repeat that. The old_string is ONE line only. The new_string ends with that same line appearing exactly ONCE, as the final line. After your edit the file must still contain exactly one occurrence of `function walkScripts(dir: string, cap: number): string[] {`, and the body of walkScripts must be untouched.\n\nDo not add or modify any import — process.env, fetch and AbortSignal are globals here. Do not touch walkMd, deriveNamingVocabulary, resolveDocsAlignTick, the live_truth object literal, or the invariants array.\n\nPURPOSE: this supplies the missing input for the accuracy invariant in docs-align-scan.ts, which is gated at line 236 on pointer.live_truth.advertised_shapes. docs-align-tick.ts builds live_truth as { existing_paths, unit_names } only — both from the local filesystem, exactly as its own comment says: \"Assemble live_truth from local filesystem only\". Because the tick never queries the discovery registry, advertised_shapes is permanently undefined and the accuracy check — the only invariant that compares a document against live truth — has never executed. A separate later change will pass this value into live_truth and add \"accuracy\" to the invariants array; do NOT do that now.\n\nEvery failure path returns an empty array deliberately, so if discovery is unreachable the accuracy branch stays exactly as inert as it is today rather than throwing. The `.replace(/\\/+$/, \"\")` endpoint normalisation matches the idiom already used in orphaned-capability-scan.ts and residual-shape-discovery.ts.",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-a6e936ba",
  "source_gap_source": "substrate_detected"
}